# Raspberry Pi 5 Docker 全自動部署完整細節紀錄

---

## 1. Mac 本地端操作

### 1.1 安裝必要工具

```bash
brew install sshpass
brew install docker
brew install docker-compose
```

### 1.2 建立自動化腳本

#### `build_image.sh`（只負責 build image）

```bash
#!/bin/bash
docker buildx build --platform linux/arm64/v8 -t hejianhangtopi:arm64 .
docker save -o hejianhangtopi_arm64.tar hejianhangtopi:arm64
```

#### `deploy_to_pi.sh`（負責傳送、安裝、啟動）

```bash
#!/bin/bash
PI_IP=192.168.99.27
PI_USER=pi
PI_PASS=0000

# 1. 清除舊的 SSH host key（避免 host key changed 錯誤）
ssh-keygen -R $PI_IP

# 2. 傳送檔案
sshpass -p $PI_PASS scp hejianhangtopi_arm64.tar $PI_USER@$PI_IP:/home/pi/
sshpass -p $PI_PASS scp docker-compose.yml $PI_USER@$PI_IP:/home/pi/
sshpass -p $PI_PASS scp .env $PI_USER@$PI_IP:/home/pi/
sshpass -p $PI_PASS scp -r prisma/migrations $PI_USER@$PI_IP:/home/pi/
sshpass -p $PI_PASS scp prisma/seed.ts $PI_USER@$PI_IP:/home/pi/
# ...其他必要檔案

# 3. 遠端自動安裝 Docker & compose
sshpass -p $PI_PASS ssh $PI_USER@$PI_IP <<'ENDSSH'
sudo apt-get update
sudo apt-get install -y docker.io docker-compose-plugin sshpass
sudo usermod -aG docker $USER
# 若第一次加群組需重開機
if ! groups | grep -q docker; then
  sudo reboot
fi
ENDSSH

# 4. 載入 image、啟動 compose
sshpass -p $PI_PASS ssh $PI_USER@$PI_IP <<'ENDSSH'
docker load -i hejianhangtopi_arm64.tar
docker compose -f docker-compose.yml up -d
ENDSSH
```

### 1.3 常用檢查指令

- 檢查檔案是否存在：
  ```bash
  ls -lh hejianhangtopi_arm64.tar docker-compose.yml .env
  ```
- 檢查 ssh/scp 是否通
  ```bash
  sshpass -p 0000 ssh pi@192.168.99.27 'echo ok'
  ```

---

## 2. Pi 5（遠端）自動化細節

### 2.1 安裝 Docker & compose（自動腳本內容）

```bash
sudo apt-get update
sudo apt-get install -y docker.io docker-compose-plugin sshpass
sudo usermod -aG docker $USER
# 若第一次加群組需重開機
if ! groups | grep -q docker; then
  sudo reboot
fi
```

### 2.2 載入 image、啟動服務

```bash
docker load -i hejianhangtopi_arm64.tar
docker compose -f docker-compose.yml up -d
```

### 2.3 常見錯誤自動修正

- **SSH host key changed**
  - 指令：`ssh-keygen -R 192.168.99.27`
- **mosquitto.conf 掛載錯誤**
  - 發現 Pi 上 mosquitto.conf 是目錄，需先刪除再傳送正確檔案
  - 指令：`rm -rf /home/pi/mosquitto.conf`
- **redis-commander、pgadmin 架構不符**
  - 修改 compose，改用 `elestio/pgadmin:latest`、`ghcr.io/joeferner/redis-commander:latest`
- **compose 服務 created 不啟動**
  - 強制啟動：`docker start <container>`
  - 查看 log：`docker logs <container>`
- **資料庫 migration/seed 問題**
  - 檢查 migration 是否存在，若無則本地產生後 scp 到 Pi
  - 指令：
    - `docker cp migrations <container>:/app/prisma/`
    - `docker cp seed.ts <container>:/app/prisma/`
    - 進入容器：`docker exec -it <container> /bin/sh`
    - 執行 migration：`npx prisma migrate deploy`
    - 執行 seed：`npx prisma db seed`

---

## 3. 重要細節修改與最佳實務

- **所有檔案傳送都用 sshpass + scp，避免手動輸入密碼**
- **所有安裝、啟動都用 sshpass + ssh，完全不碰 Pi 本機**
- **compose 內所有 image 必須為 arm64 架構，否則 Pi 會無法啟動**
- **prisma migration/seed 必須在容器內執行，且 migrations/seed.ts 要先複製進去**
- **遇到權限或目錄型態錯誤，先 rm -rf 再重新傳送**
- **所有步驟、錯誤、修正方法都記錄於本 md，方便未來複製與查詢**

---

# 完整流程一覽

1. Mac build image → save tar
2. scp image、compose、env、migrations、seed.ts 到 Pi
3. Pi 自動安裝 Docker/compose
4. Pi 載入 image、compose up
5. 若遇到錯誤，根據 log 自動修正（檔案型態、權限、image 架構、資料庫 migration/seed）
6. 服務啟動成功，瀏覽器可直接測試

---

> 本文件已涵蓋所有實際用過的指令、細節修改、常見錯誤與最佳解法，未來如遇新情境請持續補充。 

---

# 部署常見問題與解決紀錄（2025/07）

> 本區塊為 **hejianhang 專案** 實際部署經驗彙整，僅供本專案參考。

## 問題與現象

- Docker Compose 啟動時出現 container name already in use 衝突
- PostgreSQL 無法用 postgres 帳號登入，出現 role "postgres" does not exist
- 專案實際對外服務 port 應為 3001，但預設為 3000
- migration/seed 執行後資料庫仍無資料或 schema
- 重新部署後資料未更新

## 處理與解決方法

1. **Docker 容器名稱衝突**
   - 問題：`Error response from daemon: Conflict. The container name "/hejianhang-xxx" is already in use...`
   - 解法：
     - 先執行 `docker compose down` 停止所有服務
     - 再用 `docker rm -f $(docker ps -a -q --filter "name=hejianhang-")` 強制移除所有舊容器
     - 重新 `docker compose up -d`

2. **PostgreSQL 帳號問題**
   - 問題：`role "postgres" does not exist` 或 `unable to find user user: no matching entries in passwd file`
   - 解法：
     - 用 `docker exec hejianhang-postgres psql -U user -l` 查詢資料庫（-U user，非 -u user）
     - 連線帳號以 docker-compose 設定的 `user` 為主

3. **外部 port 對映錯誤**
   - 問題：外部應該用 3001 port，但預設對映 3000
   - 解法：
     - 修改 compose 檔 `ports: ["3001:3000"]`
     - 重新部署

4. **migration/seed 沒有生效**
   - 問題：資料庫有建立但沒有 schema 或資料
   - 解法：
     - 手動執行：
       - `docker exec hejianhang-app npx prisma migrate deploy`
       - `docker exec hejianhang-app npx prisma db seed`
     - 日誌顯示 `種子資料建立完成！` 才算成功

5. **重新部署流程**
   - 只要本地 build 完映像，執行 `./deploy_to_pi.sh` 即可：
     - 會自動打包、傳送、解壓、覆蓋、啟動所有服務
     - migration/seed 也會自動執行
     - 服務 port 以 compose 設定為準

## 實用指令彙整

- 查詢資料庫列表：
  ```bash
  docker exec hejianhang-postgres psql -U user -l
  ```
- 強制移除所有 hejianhang- 開頭容器：
  ```bash
  docker rm -f $(docker ps -a -q --filter "name=hejianhang-")
  ```
- 重新執行 migration/seed：
  ```bash
  docker exec hejianhang-app npx prisma migrate deploy
  docker exec hejianhang-app npx prisma db seed
  ```
- 重新部署（本地 build 完後）：
  ```bash
  ./deploy_to_pi.sh
  ```

## 小結

- 遇到部署、資料庫、port、migration 問題，皆可參考本節快速排查與修正
- 建議每次部署後都檢查 port、資料庫 schema、初始資料是否正確
- 有新問題請持續補充於本 md 

---

# Control 專案從 Docker 安裝到自動部署 control-panel 全紀錄（2025/07）

## 完整自動化流程

1. Mac 端 build arm64 映像檔並打包 tar
2. 一鍵傳送映像檔、compose、env 等必要檔案到 Pi 的 /home/pi/control
3. Pi 端自動安裝 Docker、compose（支援新版 Debian Bookworm）
4. 權限自動處理，首次安裝自動重啟，重啟後自動續接部署
5. Pi 端自動載入映像檔並啟動 docker compose，完全不需手動操作

## build_image.sh 範例
```bash
#!/bin/bash
set -e
docker buildx build --platform linux/arm64/v8 -t controltopi:arm64 --load .
docker save -o controltopi_arm64.tar controltopi:arm64
echo "映像檔已建置並打包為 controltopi_arm64.tar"
```

## deploy_to_pi.sh 範例（支援自動重啟續接）
```bash
#!/bin/bash
set -e
PI_IP=192.168.99.27      # 請改成你的 Pi IP
PI_USER=pi
PI_PASS=0000             # 請改成你的 Pi 密碼
PI_DIR=/home/pi/control  # Pi 上的專案資料夾
FLAG_FILE=/home/pi/control/.deploy_flag

ssh-keygen -R $PI_IP
sshpass -p $PI_PASS ssh -o StrictHostKeyChecking=no $PI_USER@$PI_IP "mkdir -p $PI_DIR"
sshpass -p $PI_PASS scp -o StrictHostKeyChecking=no controltopi_arm64.tar $PI_USER@$PI_IP:$PI_DIR/
sshpass -p $PI_PASS scp -o StrictHostKeyChecking=no docker-compose.yml $PI_USER@$PI_IP:$PI_DIR/
sshpass -p $PI_PASS scp -o StrictHostKeyChecking=no .env $PI_USER@$PI_IP:$PI_DIR/
# 如有其他必要檔案請依需求補上

if sshpass -p $PI_PASS ssh -o StrictHostKeyChecking=no $PI_USER@$PI_IP "[ -f $FLAG_FILE ]"; then
  echo "Pi 已重啟，繼續載入映像與啟動 compose..."
  sshpass -p $PI_PASS ssh -o StrictHostKeyChecking=no $PI_USER@$PI_IP <<ENDSSH
cd $PI_DIR
docker load -i controltopi_arm64.tar
docker compose -f docker-compose.yml up -d
rm -f $FLAG_FILE
ENDSSH
  echo "部署完成！"
  exit 0
fi

sshpass -p $PI_PASS ssh -o StrictHostKeyChecking=no $PI_USER@$PI_IP <<ENDSSH
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
sudo usermod -aG docker $USER
sudo apt-get install -y docker-compose-plugin sshpass
mkdir -p $PI_DIR
touch $FLAG_FILE
if ! groups | grep -q docker; then
  sudo reboot
fi
ENDSSH

echo "Pi 已重啟，請稍候 1-2 分鐘後再次執行 ./deploy_to_pi.sh，將自動完成部署。"
```

## docker-compose.yml 範例
```yaml
version: '3.8'
services:
  control-panel:
    image: controltopi:arm64
    container_name: control-panel
    ports:
      - "${CONTROL_PANEL_PORT:-3000}:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - DOCKER_CONTAINER=true
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./workspace:/workspace:ro
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3000/api/status"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
```

## 常見問題與解決方法

- **第一次安裝 docker 後權限未生效**
  - 解法：腳本自動重啟 Pi，重啟後再次執行 deploy_to_pi.sh 會自動續接部署
- **ssh/scp 連線卡在 yes/no**
  - 解法：所有 ssh/scp 指令加上 `-o StrictHostKeyChecking=no` 參數
- **flag 檔權限或路徑問題**
  - 解法：flag 檔用 pi 權限建立，且路徑寫死 `/home/pi/control/.deploy_flag`
- **compose image 名稱不一致**
  - 解法：compose 內 image 名稱必須與 build 的 tag 完全一致（如 controltopi:arm64）
- **docker-compose.yml version 警告**
  - 解法：可將 `version` 欄位移除，或保留不影響功能

## 實戰流程總結

1. Mac 執行 `./build_image.sh` 產生映像 tar
2. 執行 `./deploy_to_pi.sh`，Pi 會自動安裝 docker、compose，並重啟
3. Pi 重啟後再次執行 `./deploy_to_pi.sh`，自動載入映像並啟動 compose
4. 完全不需手動操作，支援多次重啟與自動續接
5. 服務啟動後可直接用瀏覽器或 API 測試

> 本區塊紀錄從 Docker 安裝到 control 面板全自動部署的所有細節、腳本、問題與最佳解法，未來如有新狀況請持續補充。 

---

# hejianhang 面板全自動部署紀錄（2025/07）

## 完整自動化流程

1. Mac 端 build arm64 映像檔並打包 tar
2. 一鍵傳送映像檔、compose、env、prisma migrations/seed.ts、mosquitto.conf 等必要檔案到 Pi 的 /home/pi/hejianhang
3. Pi 端自動安裝 Docker、compose（支援新版 Debian Bookworm）
4. 權限自動處理，首次安裝自動重啟，重啟後自動續接部署
5. Pi 端自動載入映像檔並啟動 docker compose，完全不需手動操作
6. 所有服務（app、postgres、redis、mqtt、pgadmin、redis-commander）皆為 arm64 架構

## build_image.sh 範例
```bash
#!/bin/bash
set -e
echo "[1/2] 建立 arm64 Docker 映像檔..."
docker buildx build --platform linux/arm64/v8 -t hejianhangtopi:arm64 --load .
echo "[2/2] 將映像檔打包成 tar..."
docker save -o hejianhangtopi_arm64.tar hejianhangtopi:arm64
echo "映像檔已完成並打包為 hejianhangtopi_arm64.tar"
```

## deploy_to_pi.sh 範例
```bash
#!/bin/bash
set -e

PI_IP=192.168.99.27
PI_USER=pi
PI_PASS=0000
PI_DIR=/home/pi/hejianhang

ssh-keygen -R $PI_IP
sshpass -p $PI_PASS ssh -o StrictHostKeyChecking=no $PI_USER@$PI_IP "mkdir -p $PI_DIR/prisma"
sshpass -p $PI_PASS scp -o StrictHostKeyChecking=no hejianhangtopi_arm64.tar $PI_USER@$PI_IP:$PI_DIR/
sshpass -p $PI_PASS scp -o StrictHostKeyChecking=no docker-compose.yml $PI_USER@$PI_IP:$PI_DIR/
sshpass -p $PI_PASS scp -o StrictHostKeyChecking=no .env $PI_USER@$PI_IP:$PI_DIR/
sshpass -p $PI_PASS scp -o StrictHostKeyChecking=no -r prisma/migrations $PI_USER@$PI_IP:$PI_DIR/prisma/
sshpass -p $PI_PASS scp -o StrictHostKeyChecking=no prisma/seed.ts $PI_USER@$PI_IP:$PI_DIR/prisma/
sshpass -p $PI_PASS scp -o StrictHostKeyChecking=no mosquitto.conf $PI_USER@$PI_IP:$PI_DIR/

sshpass -p $PI_PASS ssh -o StrictHostKeyChecking=no $PI_USER@$PI_IP <<ENDSSH
sudo apt-get update
sudo apt-get install -y docker.io docker-compose-plugin sshpass
sudo usermod -aG docker \$USER
if ! groups | grep -q docker; then
  sudo reboot
fi
cd $PI_DIR
docker load -i hejianhangtopi_arm64.tar
docker compose -f docker-compose.yml up -d
ENDSSH

echo "部署完成！可於 Pi 上檢查服務狀態。"
```

## docker-compose.yml 重要修正
- app 服務必須只用 image，不可有 build: .
  ```yaml
  app:
    image: hejianhangtopi:arm64
    # ... 其他設定 ...
  ```
- redis-commander 請用 arm64 版本：
  ```yaml
  redis-commander:
    image: ghcr.io/joeferner/redis-commander:latest
    # ... 其他設定 ...
  ```

## 常見問題與解決方法

- **ssh/scp 連線卡在 yes/no**
  - 解法：所有 ssh/scp 指令加上 `-o StrictHostKeyChecking=no` 參數
- **app 服務在 Pi build 失敗（找不到 Dockerfile）**
  - 解法：compose 內 app 只用 image，不要 build
- **mosquitto.conf 掛載型態錯誤**
  - 問題：Pi 上 mosquitto.conf 是目錄，導致 docker compose 掛載失敗
  - 解法：
    ```bash
    sshpass -p 0000 ssh -o StrictHostKeyChecking=no pi@192.168.99.27 "rm -rf /home/pi/hejianhang/mosquitto.conf"
    sshpass -p 0000 scp -o StrictHostKeyChecking=no mosquitto.conf pi@192.168.99.27:/home/pi/hejianhang/
    ```
- **redis-commander 架構不符**
  - 解法：image 換成 `ghcr.io/joeferner/redis-commander:latest`
- **Wi-Fi blocked by rfkill**
  - 解法：在 Pi 上執行 `sudo raspi-config` 設定 WLAN Country，或 `sudo rfkill unblock wifi`
- **docker-compose.yml version 警告**
  - 解法：可將 `version` 欄位移除，或保留不影響功能

## 服務啟動後自動執行 migration/seed（可選）
```bash
sshpass -p 0000 ssh -o StrictHostKeyChecking=no pi@192.168.99.27 "docker exec hejianhang-app npx prisma migrate deploy"
sshpass -p 0000 ssh -o StrictHostKeyChecking=no pi@192.168.99.27 "docker exec hejianhang-app npx prisma db seed"
```

## 小結
- Mac 端 build 好映像檔，Pi 端完全不需 build，所有服務皆為 arm64 架構
- 全流程自動化，無需人工互動，遇到錯誤可依本節快速排查修正
- 有新問題請持續補充於本 md 

---

# yijiaxiang 專案 Raspberry Pi 5 Docker 全自動部署紀錄（2025/07）

## 一、完整自動部署流程

1. Mac 上修改程式碼或設定檔（如 backend/config/cors.php、app/Http/Kernel.php）
2. 一鍵執行：
   ```bash
   ./build_image.sh && ./deploy_to_pi.sh
   ```
   - build_image.sh 會 build arm64 映像檔並打包 tar
   - deploy_to_pi.sh 會自動傳送所有檔案到 Pi 的 ~/yijiaxiang，並重啟所有服務
3. Pi 上自動完成：
   - 載入映像檔
   - docker compose up -d 啟動所有服務
   - 自動執行 migration/seeders
   - 完成後可直接用 http://樹莓派IP:5180 測試前端

## 二、主要腳本內容

### build_image.sh
```bash
#!/bin/bash
docker buildx build --platform linux/arm64/v8 -t yijiaxiang:arm64 -f backend/Dockerfile ./backend
docker save -o yijiaxiang_arm64.tar yijiaxiang:arm64
```

### deploy_to_pi.sh
```bash
#!/bin/bash
PI_IP=192.168.99.27
PI_USER=pi
PI_PASS=0000
PI_DIR="yijiaxiang"

# 自動寫入前端 API baseURL
sed -i '' "s|^VITE_API_BASE_URL=.*|VITE_API_BASE_URL=http://$PI_IP:8000|g" frontend/.env

# 1. 清除舊的 SSH host key
ssh-keygen -R $PI_IP

# 2. 建立遠端 yijiaxiang 資料夾
sshpass -p $PI_PASS ssh -o StrictHostKeyChecking=no $PI_USER@$PI_IP "mkdir -p ~/$PI_DIR"

# 3. 傳送檔案到 Pi 的 yijiaxiang 資料夾
sshpass -p $PI_PASS scp -o StrictHostKeyChecking=no yijiaxiang_arm64.tar $PI_USER@$PI_IP:~/$PI_DIR/
sshpass -p $PI_PASS scp -o StrictHostKeyChecking=no docker-compose.yml $PI_USER@$PI_IP:~/$PI_DIR/
sshpass -p $PI_PASS scp -o StrictHostKeyChecking=no backend/.env.example $PI_USER@$PI_IP:~/$PI_DIR/.env
sshpass -p $PI_PASS scp -o StrictHostKeyChecking=no -r backend/database/migrations $PI_USER@$PI_IP:~/$PI_DIR/
sshpass -p $PI_PASS scp -o StrictHostKeyChecking=no -r backend/database/seeders $PI_USER@$PI_IP:~/$PI_DIR/
sshpass -p $PI_PASS scp -o StrictHostKeyChecking=no -r frontend $PI_USER@$PI_IP:~/$PI_DIR/
# ...如有其他必要檔案請自行補上

# 4. 遠端自動安裝 Docker & compose
sshpass -p $PI_PASS ssh -o StrictHostKeyChecking=no $PI_USER@$PI_IP <<'ENDSSH'
sudo apt-get update
sudo apt-get install -y docker.io docker-compose-plugin sshpass
sudo usermod -aG docker $USER
if ! groups | grep -q docker; then
  sudo reboot
fi
ENDSSH

# 5. 載入 image、啟動 compose、執行 migration/seed
sshpass -p $PI_PASS ssh -o StrictHostKeyChecking=no $PI_USER@$PI_IP <<'ENDSSH'
cd ~/yijiaxiang
docker load -i yijiaxiang_arm64.tar
docker compose up -d
sleep 20
BACKEND_CONTAINER=$(docker compose ps -q backend)
if [ -n "$BACKEND_CONTAINER" ]; then
  docker compose exec backend php artisan migrate --force
  docker compose exec backend php artisan db:seed --force
else
  echo "找不到 backend 容器，無法執行 migration/seed"
fi
ENDSSH
```

## 三、常見問題與解決方法

### 1. CORS 問題
- 錯誤訊息：`No 'Access-Control-Allow-Origin' header is present on the requested resource.`
- 解法：
  - backend/config/cors.php 設定 'allowed_origins' => ['*'] 或正確的前端網址
  - backend/app/Http/Kernel.php 的 api middleware group 加入 \Fruitcake\Cors\HandleCors::class
  - 清除快取並重啟 backend：
    ```bash
    docker compose exec backend php artisan config:clear
    docker compose restart backend
    ```

### 2. Laravel 500 Internal Server Error
- 常見原因：.env 檔案缺少 APP_KEY、資料庫連線錯誤、權限問題
- 解法：
  - 複製 .env 到容器內：
    ```bash
    docker compose cp .env backend:/var/www/.env
    ```
  - 產生 APP_KEY：
    ```bash
    docker compose exec backend php artisan key:generate
    ```
  - 重啟 backend

### 3. 前端 API 連線錯誤（localhost:8000）
- 原因：frontend/.env 沒有設成 Pi 的 IP
- 解法：
  - 設定 VITE_API_BASE_URL=http://192.168.99.27:8000
  - 重新 build 前端並 deploy

### 4. .env 設定錯誤導致 SQLite 錯誤
- 錯誤訊息：Database file at path ... does not exist
- 解法：
  - .env 設定 DB_CONNECTION=mysql 並正確填寫資料庫資訊
  - 複製 .env 到容器並重啟 backend

### 5. 任何 config 或 PHP 檔案有改動都要重新 build + deploy
- 一鍵指令：
  ```bash
  ./build_image.sh && ./deploy_to_pi.sh
  ```

### 6. 商品列表前端顯示為空、載入失敗、SyntaxError: Unexpected token '<'
- 現象：商品頁面無資料，console 出現 SyntaxError: Unexpected token '<', "<!DOCTYPE "... is not valid JSON
- 原因：前端請求 `/api/v1/products` 時，實際打到前端伺服器（如 Vite dev server 或 Nginx），回傳了 index.html 而不是 API JSON。
- 解法：
  1. **前端請求必須用 API baseURL 絕對路徑**
     - 修正 ProductsView.vue 內 fetch 請求：
       ```js
       fetch(`${import.meta.env.VITE_API_BASE_URL}/api/v1/products?...`)
       ```
     - 不可用 `/api/v1/products` 或 `/products` 這種相對路徑。
  2. **重新 build 並 deploy 前端**
     ```bash
     cd frontend
     npm run build
     cd ..
     ./deploy_to_pi.sh
     ```
  3. **確認 API baseURL、docker-compose 端口、開啟網址都正確**
     - VITE_API_BASE_URL 必須指向 Pi 的 backend 8000 port
     - docker-compose.yml backend: `ports: ["8000:8000"]`，frontend: `ports: ["5180:5173"]`
     - 用 http://192.168.99.27:5180 開啟前端
  4. **Network 面板確認 API 請求網址**
     - 請求必須是 `http://192.168.99.27:8000/api/v1/products`，不能是 `/products` 或 localhost
  5. **修正後商品列表會正確顯示資料**

---

> 本區塊為 yijiaxiang 專案自動部署、常見問題與解決方法完整紀錄，未來如有新狀況請持續補充。 